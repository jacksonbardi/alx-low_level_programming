#include "main.h"

/**
  * add - adds two integers and returns the result.
  * @a: one of the two numbers to be added.
  * @b: one of the two numbers to be added.
  * Return: result of the two numbers.
  */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}

