#include "main.h"

/**
  * _abs - computes the absolute value of an integer.
  * @num: int to be checked
  * Return: return 0.
  */
int _abs(int num)
{
	if (num > 0)
	{
		return (num);
	}
	return (-num);
}
